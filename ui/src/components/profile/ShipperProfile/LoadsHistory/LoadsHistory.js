import React from 'react';
import PropTypes from 'prop-types';

const LoadsHistory = props => {
  return (
    <div>
      Here you can see all loads belonging to you
    </div>
  );
};

LoadsHistory.propTypes = {

};

export default LoadsHistory;
